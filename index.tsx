// This file ensures the application loads correctly by redirecting
// to the main entry point within the /src directory. This resolves
// potential build tool confusion between the root and src index files.
import './src/index.tsx';
